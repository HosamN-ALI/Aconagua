# المعلم الذكي - Saudi Smart Math Tutor

> منصة تعليمية ذكية لتعليم الرياضيات للمنهج السعودي باستخدام الذكاء الاصطناعي

## 📋 نظرة عامة

هذا المشروع عبارة عن منصة تعليمية متقدمة تستخدم الذكاء الاصطناعي لتدريس الرياضيات للطلاب السعوديين من الصف الأول حتى الثاني عشر. يستخدم النظام طريقة سقراط في التعليم، حيث لا يقدم الإجابات مباشرة بل يوجه الطالب للوصول إليها بنفسه.

## 🏗️ المعمارية

المشروع يتبع معمارية **Modular Monolith** التي تجمع بين بساطة الـ Monolith وفوائد الـ Microservices:

```
my-smart-teacher/
├── .blackbox/rules/          # قواعد هندسة السياق للذكاء الاصطناعي
│   ├── 01-architecture.md
│   ├── 02-tech-stack.md
│   └── 03-atomic-design.md
├── docs/
│   └── MASTER_PLAN.md        # خارطة الطريق التنفيذية
├── src/
│   ├── modules/
│   │   ├── auth/             # وحدة المصادقة والمستخدمين
│   │   ├── curriculum/       # وحدة المنهج الدراسي
│   │   ├── ai-tutor/         # وحدة الذكاء الاصطناعي (المعلم)
│   │   └── learning/         # وحدة التقدم والتعلم
│   └── shared/               # الأكواد المشتركة
│       ├── entities/
│       ├── exceptions/
│       └── utils/
├── frontend/
│   └── src/
│       ├── app/              # صفحات Next.js
│       └── components/
│           ├── atoms/        # مكونات أساسية
│           ├── molecules/    # مكونات مركبة
│           ├── organisms/    # مكونات معقدة
│           └── templates/    # قوالب الصفحات
└── prisma/
    └── schema.prisma         # مخطط قاعدة البيانات
```

## 🛠️ التقنيات المستخدمة

### Backend
- **NestJS** - إطار عمل Node.js قوي مع TypeScript
- **Prisma ORM** - للتعامل مع قاعدة البيانات
- **PostgreSQL** - قاعدة البيانات الرئيسية
- **pgvector** - للبحث الدلالي (RAG)
- **OpenAI/Claude** - نماذج اللغة الكبيرة

### Frontend
- **Next.js 14** - إطار عمل React مع App Router
- **Tailwind CSS** - للتنسيق
- **Zustand** - إدارة الحالة
- **react-katex** - لعرض المعادلات الرياضية
- **TypeScript** - لغة البرمجة

## 🚀 البدء

### المتطلبات
- Node.js 18+
- PostgreSQL 14+
- npm أو yarn

### التثبيت

1. **استنساخ المشروع**
```bash
git clone <repository-url>
cd my-smart-teacher
```

2. **تثبيت التبعيات (Backend)**
```bash
npm install
```

3. **تثبيت التبعيات (Frontend)**
```bash
cd frontend
npm install
cd ..
```

4. **إعداد البيئة**
```bash
cp .env.example .env
```

عدّل ملف `.env` وأضف:
- رابط قاعدة البيانات `DATABASE_URL`
- مفتاح OpenAI API `OPENAI_API_KEY`
- بيانات Supabase (اختياري للـ Vector DB)

5. **إنشاء قاعدة البيانات**
```bash
npx prisma generate
npx prisma db push
```

6. **تشغيل المشروع**

**Backend (Terminal 1):**
```bash
npm run start:dev
```

**Frontend (Terminal 2):**
```bash
cd frontend
npm run dev
```

الآن يمكنك الوصول إلى:
- **Frontend**: http://localhost:3000
- **Backend API**: http://localhost:3000
- **Swagger Docs**: http://localhost:3000/api/docs

## 📚 الوحدات الأساسية

### 1. Auth Module
مسؤولة عن:
- تسجيل المستخدمين (طلاب ومعلمين)
- المصادقة باستخدام JWT
- إدارة الأدوار والصلاحيات

### 2. Curriculum Module
مسؤولة عن:
- هيكل المنهج السعودي (مواد، صفوف، فصول، دروس)
- التمارين والأسئلة
- **قاعدة مهمة**: هذه الوحدة لا تعتمد على أي وحدة أخرى

### 3. AI Tutor Module (الدماغ)
مسؤولة عن:
- التواصل مع الطالب باستخدام طريقة سقراط
- RAG (Retrieval Augmented Generation) للبحث في المنهج
- OCR لاستخراج المسائل من الصور
- تخزين محادثات الطلاب

### 4. Learning Module
مسؤولة عن:
- تتبع تقدم الطلاب
- نظام النقاط والشارات (Gamification)
- سجل المحاولات في الاختبارات
- لوحة المتصدرين

## 🎨 Atomic Design (Frontend)

المشروع يتبع نظام Atomic Design لتنظيم المكونات:

### Atoms (الذرات)
مكونات أساسية لا يمكن تقسيمها:
- `Button` - أزرار
- `Input` - حقول الإدخال
- `Badge` - الشارات
- `MathFormula` - عرض معادلات LaTeX

### Molecules (الجزيئات)
مجموعة من الذرات:
- `SearchBar` - شريط البحث
- `ChatMessage` - رسالة محادثة
- `QuestionCard` - بطاقة سؤال

### Organisms (الكائنات)
أقسام معقدة من الواجهة:
- `ChatInterface` - واجهة المحادثة الكاملة
- `CurriculumTree` - شجرة المنهج

### Templates (القوالب)
تخطيطات الصفحات:
- `DashboardLayout` - تخطيط لوحة التحكم
- `StudentLayout` - تخطيط صفحة الطالب

## 🤖 كيف يعمل الذكاء الاصطناعي؟

1. **الطالب يسأل سؤالاً**
2. **RAG Pipeline**:
   - البحث عن محتوى ذي صلة في قاعدة بيانات المنهج (Vector Search)
   - استرجاع الدروس والتمارين المرتبطة
3. **بناء الـ Prompt**:
   - إضافة تعليمات المعلم السقراطي
   - إضافة السياق من المنهج
   - إضافة تاريخ المحادثة
4. **إرسال إلى LLM** (GPT-4 أو Claude)
5. **إرجاع الإجابة** بأسلوب توجيهي وليس مباشر

## 📝 قواعد البرمجة الهامة

### 🚫 ممنوع منعاً باتاً:
1. استيراد وحدة من وحدة أخرى مباشرة (يجب استخدام الـ Public API فقط)
2. إضافة تبعيات في الـ Shared Kernel على أي وحدة
3. جعل وحدة `curriculum` تعتمد على `ai-tutor` أو `learning`

### ✅ يجب:
1. استخدام `Result<T>` للإرجاع من الخدمات
2. استخدام TypeScript Strict Mode
3. توثيق جميع الـ Controllers بـ Swagger
4. اتباع Atomic Design في Frontend
5. استخدام Tailwind CSS (Mobile-First)

## 🗺️ خارطة الطريق

راجع ملف `docs/MASTER_PLAN.md` لتفاصيل المراحل التطويرية:

- ✅ **المرحلة 1**: البنية التحتية والهيكل
- 🔄 **المرحلة 2**: وحدة المنهج وملء البيانات
- ⏳ **المرحلة 3**: تطوير الذكاء الاصطناعي (RAG + OCR)
- ⏳ **المرحلة 4**: الواجهة الأمامية (Atomic Components)

## 🧪 الاختبار

```bash
# تشغيل الاختبارات
npm test

# اختبارات التكامل
npm run test:e2e
```

## 📖 الوثائق الإضافية

- [قواعد المعمارية](.blackbox/rules/01-architecture.md)
- [التقنيات المستخدمة](.blackbox/rules/02-tech-stack.md)
- [Atomic Design](.blackbox/rules/03-atomic-design.md)
- [Master Plan](docs/MASTER_PLAN.md)

## 🤝 المساهمة

نرحب بالمساهمات! يرجى:
1. قراءة قواعد المعمارية في `.blackbox/rules/`
2. اتباع معايير البرمجة المذكورة
3. إنشاء Pull Request مع شرح واضح

## 📄 الترخيص

[اختر الترخيص المناسب]

## 📞 التواصل

لأي استفسارات أو اقتراحات، يرجى فتح Issue في GitHub.

---

**تم البناء بـ ❤️ لخدمة التعليم السعودي**
